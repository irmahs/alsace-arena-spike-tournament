/**
 * Feature flags. Flip a value here to turn a feature on — no other code changes needed.
 */

/** Scan a scoreboard screenshot with Claude vision in the admin score-entry form. */
export const SCOREBOARD_SCAN_ENABLED = false;
